# Wallpapers

Wallpaper images are not vendored in this repo (large binaries don't
belong in dotfiles).

Drop your images in `~/Pictures/Wallpapers/` (created by `install.sh`).
The Liquid Glass bar is tuned for dark, low-texture wallpapers —
busy bright images wash out the frosted pills.
