# Fallback when matugen has never run (install.sh copies, never symlinks,
# so `matugen image ...` can overwrite ~/.config/kitty/colors.conf freely).
# Matches kitty.conf static defaults.
background #101218
foreground #e8eaf0
cursor #e8eaf0
selection_background #2a2e3a
selection_foreground #e8eaf0
